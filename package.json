{
  "name": "prevu",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "cli": "cargo run --manifest-path cli/Cargo.toml --"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
